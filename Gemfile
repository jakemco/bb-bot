# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# simple secrets management via environment
gem 'dotenv'

# discord API
gem 'discordrb'

# ORM
gem 'pg'
gem 'sequel'
gem 'sqlite3'

# fancy debug printing
gem 'awesome_print'
